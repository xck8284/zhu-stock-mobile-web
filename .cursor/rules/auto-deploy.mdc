---
description: 修正完成後自動 commit、push，觸發 Render/Vercel 部署
alwaysApply: true
---

# 自動更新與部署

本專案含兩個 Git 倉庫，修正後必須自動推送以觸發線上部署：

| 倉庫 | 路徑 | 部署目標 |
|------|------|----------|
| 前端 | 本 repo（`zhu-stock-mobile-web`） | Vercel（push `main` 自動部署） |
| 後端 | `C:\Users\user\Desktop\zhu-stock-app` | Render（push `main` 自動部署） |

## 完成程式修正後必做

1. 確認變更範圍（前端 / 後端 / 兩者）。
2. 執行 `scripts/auto-push-deploy.ps1`（或等價的 git add / commit / push）。
3. Shell 需請求 `git_write` 與 `network`（或 `full_network`）權限。
4. 推送成功後告知使用者：Render / Vercel 約 1～3 分鐘內完成部署。
5. 若改動後端分析邏輯，部署完成後可提示使用者按「立即更新分析」或等待 16:05 自動分析。

## Commit 訊息

- 簡短說明「為什麼」，一句話即可。
- 前後端分開 commit，訊息各自對應變更內容。

## 禁止推送

- `.env`、金鑰、credentials
- `__pycache__/`、`node_modules/`

## 可選：對話結束自動推送

若要在 Agent 結束時自動執行推送，可在 `.cursor/hooks.json` 加入 `stop` hook 呼叫 `scripts/auto-push-deploy.ps1`（需本機 Cursor 設定允許）。
